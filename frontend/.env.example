# Base URL of the backend API.
# Leave empty for local development — the Vite dev server proxy handles routing.
# Set to the full backend URL for production builds (e.g. Azure Container Apps URL).
VITE_API_BASE_URL=
